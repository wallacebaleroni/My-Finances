export default class Account {
    constructor(account_id, name, type, color) {
        this.account_id = account_id
        this.name = name
        this.type = type
        this.color = color
    }
}
